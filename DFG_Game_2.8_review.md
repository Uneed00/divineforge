# DFG_Game_2.8 코드 리뷰

검토 대상: `DFG_Game_2.8.html`

## 요약
- 전체적으로 게임 로직(핸드/시장/제작/신화 능력/AI/시뮬레이터)이 한 파일에 집약되어 있으며, 기능 범위가 매우 넓습니다.
- 즉시 수정이 필요한 **중요 버그 2건**과 유지보수성 이슈를 확인했습니다.

---

## 주요 이슈 (우선순위 순)

### 1) `SimulationManager.resetStats()`가 중복 선언되어 통계 구조가 깨짐 (Critical)
- `resetStats()`가 클래스 내부에 두 번 정의되어 있습니다.
- JS 클래스 메서드 특성상 **나중 선언이 앞 선언을 덮어쓰기** 때문에, 앞에서 초기화하던 `godWins/godWins2/godWins3` 및 상세 누적치가 사라집니다.
- 이후 `updateUI()`는 해당 필드를 참조하므로 시뮬레이션 리셋 후 UI 표기 불일치/런타임 오류 가능성이 있습니다.

**근거 위치**
- 첫 번째 선언: `DFG_Game_2.8.html` L3430-L3442
- 두 번째 선언(실제 적용됨): `DFG_Game_2.8.html` L3444-L3448
- 해당 필드 사용: `DFG_Game_2.8.html` L4119-L4151

**권장 조치**
- `resetStats()`를 하나로 통합하고, `constructor`와 동일한 stats 스키마를 재사용하도록 상수화하세요.

---

### 2) `canAct(cost)`가 전달된 `cost`를 사용하지 않아 행동 가능 판정이 부정확함 (High)
- `canAct(cost)`는 인자를 받지만 실제로는 `this.cp.time >= CONFIG.MAX_TIME`만 검사합니다.
- 예: 시간이 11이고 비용이 3인 행동도 허용됩니다.
- 현재 `consumeTime`이 이후 턴 정렬로 흡수하더라도, UX/룰 관점에서 “남은 시간 대비 행동 가능 여부”와 표시가 어긋날 수 있습니다.

**근거 위치**
- 함수 정의: `DFG_Game_2.8.html` L2270-L2274
- 시간 소모 처리: `DFG_Game_2.8.html` L1095-L1106

**권장 조치**
- `canAct(cost)`에서 `this.cp.time + cost > CONFIG.MAX_TIME` 체크를 추가하세요.
- 비용 0(pass) 케이스는 예외 처리 분기 권장.

---

## 구조/유지보수 개선 제안

### A) 단일 HTML 파일(4,400+라인) 과밀
- UI/CSS/도메인 로직/AI/시뮬레이터가 한 파일에 존재하여 회귀 위험이 큽니다.
- 단계 분리 권장:
  1. 데이터(DB/상수)
  2. 게임 엔진(`Game`)
  3. AI 전략
  4. 시뮬레이터
  5. UI 렌더러

### B) 설정값 하드코딩 중복
- 일부는 `BALANCE_CONFIG`로 잘 추상화되어 있으나, 여전히 직접 숫자 하드코딩이 섞여 있습니다.
- 핵심 수치(시장 크기/핸드 제한/턴 비용/신화 단계 수치)는 전부 설정 객체로 집약하는 것이 안전합니다.

### C) 디버그/실서비스 코드 경계 불명확
- 개발자 모드/시뮬레이터 UI가 실게임 DOM에 항상 상주합니다.
- 빌드 플래그 또는 URL 파라미터로 분리하면 오작동 범위를 줄일 수 있습니다.

---

## 총평
- 기능 밀도와 구현 난이도는 높고, 신화 능력/AI 연동이 인상적입니다.
- 다만 현재는 “동작은 하지만 유지보수 시 깨지기 쉬운” 구간이 존재합니다.
- 우선은 **중복 `resetStats` 제거**와 **`canAct` 판정 보정**부터 적용하는 것을 권장합니다.
